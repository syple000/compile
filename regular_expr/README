正则解析： 解析正则表达式，对特定字符串，若匹配返回true; 
    对特定字符，由当前状态开始，转移并返回下一个状态
    
    基于DFA实现，原生支持 连接，或，星号 等规则，更多功能扩展可由StandardizeExpr实现
    原生保留符号： 
        . | * ( )     可由\转义作为平凡符号
    项目中扩展后的特殊符号： 
        [ ] \s等

匹配示例： 
    (a|bc)*可以匹配： aaa， abcbcaa等