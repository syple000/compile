#include <vector>
#include <string>

#ifndef CODE_ANALYSIS_VARIABLE
#define CODE_ANALYSIS_VARIABLE

/*
    1. 代码类型可以细分为基本类型(**)， 自建类型(**)， 函数类型(returnType:arg1Type:...  可以多次出现). 由3解析函数统一处理获取，以统一格式存储
*/
struct VariableType {
};

struct Variable {
};

#endif