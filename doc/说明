该文件夹下的文件为辅助文件， 不参与编译
根据需要，将该文件的代码迁移到expr_file文件进行使用

存放的文件：
    vartable： 
        tableName,
        变量存储
            变量名（对于函数需要拼凑该名），
            变量类型（指针及基本类型：类型名， 自定义类型： tableName级联， 函数： $返回值类型：参数类型）， 
            数组信息（如果不是数组，则记录array(0)），
            变量空间大小，
            偏移量，
            可见性
            附加信息：对于指针，给出指向对象类型与堆地址
        是否是类型或函数或普通符号表
        可见性
        偏移量开始计数大小
        父空间可见程度

初次设计：简单脚本语言
包含：
    类型定义： def typename {} 声明时直接var = typename;
    函数定义: def typename funcname (params) {} : type可以为void
    变量声明： typename varname (= typename/表达式/基本类型对应的值（1, true, null...）)
    语句块： 
    表达式： 有返回值的表达式， 无返回值的表达式（void）

